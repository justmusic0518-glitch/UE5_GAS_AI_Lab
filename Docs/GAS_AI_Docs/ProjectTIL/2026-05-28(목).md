# TIL 적을 내용
1. FInputValue 사용법
2. FVector direction = currentLockOnTarget->GetActorLocation() - GetActorLocation(); direction.Z = 0.0f; // 왜 Z는 0이지? 점프같은거 하면 어캄 const FRotator targetRotation = direction.Rotation(); 이것도 이해가 안가는게 direction의 Rotation을 왜구하나ㅡㄴ거임? direction은 distance 같은데 Actor와 TargetActor사이의 거리
3. ActivateAbility()
4. GiveAbility(FGameplayAbilitySpec()) 이거 Spec은 뭐고 뒤에 붙는 인자들은 머임? 또 Give Ability라는게 멀까
TryActivateAbility = 스킬 사용 버튼 누름
ActivateAbility = 스킬 내용 실제 실행
5. abilitySystemComponent->TryActivateAbilityByClass(UGAPlayerAttack::StaticClass());
6. **Ability 안에서 캐릭터 가져오기**
7. 







# 추가 질문

1. BindAciton을 Started로 해놔야하나 좌클 Attack이나 Dodge같은거는 한번 눌리는거는? Triggerd는 계속 눌리던데
2. 왜 굳이 TSubclassOF로하지? 전에 class가져올때도 TSubclassOf로 가져왔는데? Instance같은거는 TObjectPtr로 가져오잖아 이거랑 똑같이 하면 안댐? 왜 굳이 GameplayAbilityBase를 이걸로 가져오는겅미
3. Base에 작성하는것보다 데미지 적용 로직 같은거는 Component같은거에서 하는게 별로인감
4.     FTimerDelegate::CreateWeakLambda(OwnerCharacter, [OwnerCharacter, Handle, ActorInfo, ActivationInfo, this]()
    {
        OwnerCharacter->SetInvincible(false);
        UE_LOG(LogTemp, Warning, TEXT("Dodge End: Invincible OFF"));

        EndAbility(Handle, ActorInfo, ActivationInfo, true, false);
    }),

이거 Lambda는 C++에서 대충 아는데 CreateWeakLambda는 어떤거고 앞에 OwnerCharacter는 왜들가고 뒤에는 캡쳐인거 알긴하는데 this만 적으면 캡쳐를못하나? 그리고 EndAbliity를 여기에 왜넣어두는거지 함수 막줄에만 넣으면 되는거아니가 이거 뭐 return도 아니고 끝나면 어차피 함수 막줄 할텐데


# 오늘 할 것
1. 피드백 받은 내용 확인하기
2. 알고리즘 두개 풀기
3. Tick이랑 LockOn이랑 연결해서 만들어보기
GetAllActorsWithTag는 프로토타입으로 유지하되, 나중에 LockOnTargetComponent나 인터페이스로 바꿀 수 있게 함수 하나로 감싸기.



# 개념 정리 해둘 것
1. 플레이어 이동별로 정리. bOr뭐 이런거랑 방향으로 움직이거나, backView TopView로 보는거ㄹ
2. 보간 인자 정리
3. FoundActor 정리 Num으로 체크하는것까지
4. EQS쓰는건 별로다 EQS를 따라하는게 낫다
5. BIndActoin 구현. 뭐 Move나 LockOn 



이따가 브로셔 피드백 받을꺼
	작동원리나 코드쪽에서 내용을 보여줘야한다면 핵심 기능에 적을지 핵심기술에 적을지 
	핵심 기술에는 어떤게 있을까요? 



# 더 구현 해보고 싶은것
1. 캐릭터 손에 칼 부착. 
2. 애니메이션과 이펙트 타점 맞추기
3. 원하는 메쉬 가져와서 원하는 애니메이션 넣어보기
4. https://dev.epicgames.com/documentation/unreal-engine/motion-warping-in-unreal-engine
5. https://dev.epicgames.com/documentation/unreal-engine/motion-matching-in-unreal-engine
6. https://dev.epicgames.com/documentation/unreal-engine/BlueprintAPI/ContextualAnimationSystem
7. 위 링크 어려운건가? 이거 적용해볼 수 있나? 블루프린트로 해야하는건가? 실무에서 많이 쓰이는 방식인가? 이게 정석인가?




